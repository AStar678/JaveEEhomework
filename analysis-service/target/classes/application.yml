server:
  port: 8083

spring:
  application:
    name: analysis-service
  datasource:
    # 数据库配置占位符
    url: jdbc:mysql://localhost:3306/db_analysis?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: password
