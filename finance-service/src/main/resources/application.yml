server:
  port: 8082

spring:
  application:
    name: finance-service
  datasource:
    # 数据库配置占位符
    url: jdbc:mysql://localhost:3306/db_finance?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: password
